# openmeetings-docker
Docker image for OM

## TIPS:

* to build: 
```docker
docker build .
```
* to run OM:
```docker
docker run -it __HASH__
```
* to enter machine:
```docker
docker run -it __HASH__ bash
```
